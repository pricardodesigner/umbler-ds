# Skill bundle (build artifact)
umbler-ds.skill
umbootstrap-ds.skill

# Plugin build artifacts (.plugin = zip empacotado, regerado a cada release)
umbler-talk.plugin
umbler-design.plugin

# Skill source (editado via skill folder, não precisa ficar no repo do HTML)
umbootstrap-ds/
umbootstrap-ds-workspace/

# Planos / notas locais
umbootstrap-build-plan.txt

# Outputs gerados por mim no Cowork
skill-validation.html
push-to-github.sh

# OS
.DS_Store
.claude/
